import 'package:flutter/material.dart';

class AppColors {
  // Main background
  static const Color background = Color(0xFF1A1A1A);
  
  // Surface colors (cards, dialogs, bottom nav)
  static const Color surface = Color(0xFF242424);
  static const Color surfaceLight = Color(0xFF303030);

  // Accents
  static const Color primaryGold = Color(0xFFD4AF37);
  static const Color primaryGoldLight = Color(0xFFE5C665);
  static const Color primaryGoldDark = Color(0xFFB5952F);
  
  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textHint = Colors.white38;
  
  // States
  static const Color error = Color(0xFFCF6679);
  static const Color success = Color(0xFF4CAF50);
  
  // Distinct dividers
  static const Color divider = Color(0xFF3A3A3C);
}
